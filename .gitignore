/target
.target-check/
.target-check2/
*.db
*.db-shm
*.db-wal
.env
.env.local
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Agent/IDE config (private)
CLAUDE.md
.claude/
.codex
.worktrees/
WORKTREE-SPEC.md
PLAN.md
_parts/
.test-llm/

# Internal docs
docs/plans/
docs/specs/
docs/benchmarks/

# Node
node_modules/
gui/build/
gui/.svelte-kit/

# Temp/test files
clippy_output.txt
migration-data/
data/
kleos-lib/data/lance/
migration-test/
.agent-forge-tmp/

# Local cargo config (per-machine target-dir overrides, etc.)
.cargo/config.toml

# Dev utility stubs
test.py
write_files.py
_gen_memory.py
__pycache__/
.persona/
GROWTH.md

# Local agent tooling state (per-session activation, never in project repos)
frameshift.toml
frameshift.lock
.wrangler/

# Graphify generated output (local knowledge graph, regenerated on demand)
graphify-out/

# Backup snapshots of deployed binaries (kept locally for rollback)
backup/
KLEOS.md

# docs/dev-notes : travail en cours / fait, NON public (depot public).
# Seul local-patches.md reste tracke (reference des patches VOCSAP).
docs/dev-notes/*
!docs/dev-notes/local-patches.md

# Plan/TODO/progress artefacts (racine, hors dev-notes)
task_plan.md
progress.md
findings.md
session-checkpoint.md

# Project-scoped MCP server config -- path command is poste-specific (cf. Patch 18)
.mcp.json
/.forge
.deploy-patch21.sh
# Repomix
kleos-pack.*
repomix.config.json

# Debug artifacts
dbgmd-*
